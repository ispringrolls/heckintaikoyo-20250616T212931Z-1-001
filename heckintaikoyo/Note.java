package heckintaikoyo;

class Note {
        int x; // x position of the note
        int type; // type of note (0 for don or 1 for katsu)
        public Note(int x, int type) {
            this.x = x; 
            this.type = type;
        }
    }
    